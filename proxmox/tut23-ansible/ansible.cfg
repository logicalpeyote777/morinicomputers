# ansible.cfg — configuración local del proyecto ansible-pyme
# host_key_checking=False es cómodo en laboratorio (VMs que nacen y mueren);
# en producción, con máquinas estables, déjalo activo.
[defaults]
inventory = inventario.ini
host_key_checking = False
